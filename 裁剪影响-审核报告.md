# 裁剪影响 · 交叉审核报告（公开仓库裁剪后是否影响他人理解）

> 任务：看板 t_94e4fdd5（交叉审核重跑，上次 reason=error 中断于对比阶段）
> 审核方：DSH · deepseek-v4-flash（默认参数）
> 审查对象：`/home/taxol/backup-DSH-Hermes-20260814/`（git 提交 `c4895cf`「Initial release」，22:38）
> 对比基准：`/home/taxol/DSH/`（`docs/DSH-Hermes-协作方案.md` v1.3.6、`review/`、`github-prep/`）
> 审核时间：2026-08-14 · 只读审查，未修改审查对象

---

## 总体结论（先说结论）

**无阻断项。** 裁剪后的仓库文档链条完整、基本自洽，新人按 README → INSTALL → install.sh → 插件 README → hermes-side/README.md 可以独立理解"项目干什么、怎么装、怎么用"，无需生产版内部文档。

**重要 2 项**（均为链接/残留问题，改一行即可）：
1. 插件 README（`dsh-side/plugins/dsh-kanban-watcher/README.md`）引用技能路径 `hermes-side/dsh-send/SKILL.md` 不存在（实际为 `hermes-side/dsh-send-skill/SKILL.md`）——按文档找文件会落空。
2. 保留文档 `dsh-side/docs/DSH-Hermes双Agent协作管道-能力盘点与可行性.md` 末行"详见 `docs/DSH-Hermes-协作方案.md`"指向已删文件（死链）。

**建议 8 项**（见第五节，不影响安装使用，但提升新人体验与决策信息）。

**六项关键设计要点全部有说明，无缺口**（外部事件注入原理 / done 文件安全边界 / seen baseline / 串行执行 / hermes-trusted 权限 / 版本绑定——覆盖位置见第二节）。

---

## 一、新人视角（优先级 1）

### 结论：能理解，不阻断

| 文件 | 评价 |
|---|---|
| `README.zh-CN.md` | 讲清了"是什么/适合什么/架构/快速开始/使用示例/已知限制"。架构图把 trigger 文件、done 文件、外部事件注入三条链路画全。 |
| `INSTALL-安装指南.md` | 面向全新环境，步骤完整（装 Agent → clone → 建板 → 装插件 → 打 patch → 部署技能 → systemd → 验证 → 已知坑），还带一键脚本入口与 patch 冲突处理。 |
| `dsh-side/plugins/dsh-kanban-watcher/README.md` | 插件级文档，含工作原理图、全配置项表、安全权衡必读。 |
| `hermes-side/README.md` | 补全 Hermes 侧 patch 的原理与安全设计（含 done 文件安全边界）。 |

### 新人会困惑的点（按影响排序）

1. **[建议] README 内部自相矛盾：轮询间隔 10s vs 30s。**
   `README.zh-CN.md` 已知限制写"**10s 轮询** + fs.watch：准实时"（line 128），但同文件架构节、插件 README、INSTALL 配置均为"**30s 兜底轮询**"（`pollIntervalMs: 30000`）。"10s" 是早期纯轮询设计的残留值，应统一为 30s。
2. **[建议] 使用示例的 `--model deepseek-v4-pro` 有误导。**
   `README.zh-CN.md` line 104 注释"DSH 用 pro 执行重任务"，但插件 `modelMap` 只映射 flash + `__fallback__`（均指向 flash），pro 实际会 fallback 到 flash 执行——"重任务用 pro"不成立。要么在示例里注明 fallback 行为，要么换示例模型。
3. **[建议] 插件 README 安装路径 `file:$DSH_WORKSPACE/plugins/dsh-kanban-watcher` 只对本机成立。**
   生产版仓库恰好在 `$DSH_WORKSPACE` 下才成立；外人 clone 仓库到任意目录时该路径无效。INSTALL 用 `<你的clone路径>/dsh-side/plugins/...` 是对的——插件 README 应改为同样的写法。
4. **[建议] 完成通知的机制说明分散，README 相关文档节没列 `hermes-side/README.md`。**
   done 文件安全边界、seen baseline 的完整说明在 `hermes-side/README.md`，README 只在仓库结构里一笔带过、相关文档节未列链接。建议在"相关文档"节补一行。

### 明确不构成困惑的点（已核对）

- 路径脱敏：全库已用 `$VAR` 占位符（如 `$DSH_WORKSPACE`、`$HERMES_BIN`），无真实用户名/路径残留（抽查 .md/.sh/.js/.yml/.patch/.service 仅见 `/home/alice`、`/mnt/c` 占位）。
- "插件更新必须重启 dsh web"、"seen baseline 时序"、"hermes-trusted 安全权衡"等实操坑在 INSTALL/插件 README 中均有显式警告。
- 会话继承 `--resume` 标注为"已设计、实施中、尚未可用"（SKILL.md），未虚报功能。

---

## 二、六项关键设计要点覆盖核对（优先级 2）

| 要点 | 是否覆盖 | 位置 |
|---|---|---|
| 外部事件注入原理 | ✅ | README 架构图 + 机制#4（done 文件 → Hermes 空闲循环 `_drain_done_notifications` → 【外部通知】）；`hermes-side/README.md`「工作原理」；INSTALL 第 5 步（patch 应用） |
| done 文件安全边界 | ✅ | `hermes-side/README.md`「安全设计」：done 文件视为**不可信输入**、只读 id/title/result 三字段、值截断+剥控制字符、**从不执行**、功能默认关闭 |
| seen baseline | ✅ | `hermes-side/README.md`（启动建 baseline、已有文件不重放）+ INSTALL 已知坑 #2（验证链路时序） |
| 串行执行 | ✅ | README 已知限制 #1；插件 README「工作原理」（同一时刻只跑一个，后续排队，v1 设计） |
| hermes-trusted 权限 | ✅ | 插件 README 配置表（`permissionPreset` = danger-full-access + never）+「⚠️ 安全权衡（必读）」；INSTALL 已知坑 #5 |
| 版本绑定 | ✅ | INSTALL 已知坑 #6（插件依赖 `@deepseek-ai/dsh-*@0.1.0-rc.6`，以 profile 内实际版本为准）；插件 `package.json` 依赖声明与之一致 |

**结论：六项全覆盖，无缺口，无需补文件。** 唯一可优化的是可发现性：done 文件安全边界/seen baseline 的正文在 `hermes-side/README.md`，建议在 README 相关文档节补其链接（见第一节困惑点 4）。

---

## 三、死链核查（优先级 3）

### 三份核心文件（README / INSTALL / 插件 README）

| 文件 | 引用 | 状态 |
|---|---|---|
| `README.md` / `README.zh-CN.md` | `README.md`↔`README.zh-CN.md`、`NOTICE-借鉴与合规.md`、`INSTALL-安装指南.md`、`dsh-side/docs/能力盘点...md`、插件 README、`hermes-side/PR-提交说明.md`、install.sh | ✅ 目标均存在（已对照 git 跟踪清单逐一核验） |
| `INSTALL-安装指南.md` | `hermes-side/quick_commands.yml`、`hermes-side/dsh-web.service`、`hermes-side/dsh-send-skill`、`hermes-side/PR-提交说明.md`、插件包 | ✅ 均存在 |
| `dsh-side/plugins/dsh-kanban-watcher/README.md` | **`hermes-side/dsh-send/SKILL.md`（line 118）** | ❌ **路径不存在**（实际 `hermes-side/dsh-send-skill/SKILL.md`，或镜像 `dsh-side/hermes-side/dsh-send/SKILL.md`）→ **重要** |

> 注：审查期间存在过的 `插件独立仓库-dsh-kanban-watcher/`（与 dsh-side 插件 README 内容相同，含同样错链）在 git 提交时被移除，未进入最终仓库；错链问题以已跟踪的 dsh-side 副本为准。

### 保留文档中的同类残留（超出三份核心文件，但同属"引已删文件"）

| 位置 | 引用 | 状态 |
|---|---|---|
| `dsh-side/docs/能力盘点与可行性.md` line 191 | "详见 `docs/DSH-Hermes-协作方案.md`" | ❌ 协作方案已删 → **重要** |
| `NOTICE-借鉴与合规.md` line 13 | "本地审查副本在 `review/`" | ❌ `review/` 已删 → **建议**（删除该括注即可） |
| `hermes-side/dsh-send-skill/SKILL.md` line 32 | "（§5.6）" | ⚠️ 指向已删协作方案 §5.6，悬空章节引用 → **建议**（改为一句自含说明） |

---

## 四、对比生产版设计决策（优先级 4）

被删文档（协作方案 v1.3.6、分工与目录、review/、spikes/、github-prep/）中的设计决策，经逐项比对，**没有"外人理解项目必需"级别的缺失**——核心机制（触发文件、done 文件、串行、崩溃恢复、模型翻译、白名单）均已在公开文档中表达。以下 3 条为"了解背景更有帮助"级别的决策上下文，一句话级：

1. **[建议] 为何自研 watcher 而非复用社区 MCP server（路线 A/B 决策）。**
   生产版 §1.2：`@chushixixin/dsh-harness-mcp-server` 依赖 `^0.0.1-rc.1` 与 DSH profile `0.1.0-rc.6` semver 不匹配（装上会双份单例冲突）、任务队列纯内存（重启即丢）、会话按 cwd 复用（非每任务新对话）——这是外人评估"能否换现成方案"时的关键信息。README 目前只提了该项目的名字（NOTICE 也只说思路借鉴），建议在 README 或 NOTICE 补一句"为何不用"。
2. **[建议] Hermes gateway 自带 kanban watcher 存在抢任务风险（生产版 F14）。**
   生产版要求 board/assignee 隔离、不要在该 board 上启用 Hermes dispatcher。公开文档只写了 board=assignee=dsh 配置（原子 claim 已自保护），没写这个部署注意点；对开着 gateway 的部署者有价值。
3. **[建议] 过渡性胶水定位。**
   生产版明确"预期寿命 = DSH 官方 ACP/JSON-RPC 完善前，入口与执行解耦"。外人长期采用判断需要这一句。

另已核对的非缺失项：互重启铁律（README 设计原则有）、落点体系"任务自建文件夹"（SKILL.md 有）、会话继承未实现（SKILL.md 诚实标注）、幂等/崩溃恢复（插件 README 有）。

---

## 五、分级意见汇总

**阻断（0 项）**：无。裁剪后文档链完整，新人可独立理解与安装。

**重要（2 项）**：
1. 插件 README line 118 技能路径错链：`hermes-side/dsh-send/SKILL.md` → 改 `hermes-side/dsh-send-skill/SKILL.md`。
2. `dsh-side/docs/能力盘点...md` line 191 死链：删掉"详见 `docs/DSH-Hermes-协作方案.md`"或改为自含表述。

**建议（8 项）**：
1. README 已知限制"10s 轮询"统一为 30s（与配置/其余文档一致）。
2. README 示例 `--model deepseek-v4-pro` 注明会 fallback 到 flash（modelMap 无 pro）。
3. 插件 README 安装路径 `file:$DSH_WORKSPACE/...` 改为 `<你的clone路径>/dsh-side/plugins/...`（INSTALL 写法为准）。
4. README"相关文档"节补 `hermes-side/README.md` 链接（done 安全边界/seen baseline 完整说明所在）。
5. NOTICE line 13 删除"本地审查副本在 review/"括注。
6. SKILL.md line 32 "（§5.6）"改为自含说明（删除悬空章节引用）。
7. 补一句"为何不用现成 dsh-harness-mcp-server"（版本不匹配/内存队列/会话复用）。
8. 补一句部署注意：Hermes gateway 侧有 kanban watcher，勿在该 board 启用 dispatcher；及"过渡性胶水"定位。

---

## 六、过程观察（供流程参考）

- **审查对象在审查期间被并发修改**：审查开始时备份目录无 `.git`、含 `插件独立仓库-dsh-kanban-watcher/`；审查中（22:38）出现 git 提交 `c4895cf`（Initial release），独立插件仓库目录被移除。三份核心审查文件（README/INSTALL/插件 README）内容未变，结论不受影响；`插件独立仓库`副本的错链问题以已跟踪副本为准。
- 建议：正式发布前**冻结仓库**，按本报告第五节修完 2 项重要 + 建议项后重跑一次终检；`dsh-side.tar.gz`/`hermes-side.tar.gz` 已被 .gitignore 排除（`*.tar.gz`），不会随 GitHub 发布，无需处理。
